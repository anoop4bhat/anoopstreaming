{
  "name": "prebuilt-ui",
  "version": "1.0.0",
  "description": "demo of Daily's prebuilt UI for video calls",
  "scripts": {
    "dev": "http-server"
  },
  "devDependencies": {
    "http-server": "^0.12.3"
  }
}
